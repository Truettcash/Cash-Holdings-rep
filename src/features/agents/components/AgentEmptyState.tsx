import React from 'react';

export default function AgentEmptyState() {
  return (
    <div style={{ padding: 12 }}>
      <h4>No agent runs yet</h4>
      <p>Use the composer to ask the agent to analyze the selected context.</p>
    </div>
  );
}
