# Security Settings

This repository includes configuration and workflows to help secure the project. Recommended settings to turn on in the repository security settings:

- Code scanning (CodeQL)
- Dependabot alerts and updates
- Secret scanning
- Push protection

Use the GitHub UI or the `gh` CLI to apply the settings. Example commands are included in other files in this directory.
