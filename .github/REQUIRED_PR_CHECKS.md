Recommended required PR checks

- `CI` (unit tests via Vitest)
- `E2E Tests` (Playwright)
- `CodeQL` (security analysis)

Set these in Branch protection rules under "Require status checks to pass before merging".

You can list the checks using the Checks API or see their names after a run completes.
