# Dependabot

Dependabot is configured via `.github/dependabot.yml` for weekly npm updates.

To enable: ensure Dependabot is enabled in repository settings (Security & analysis → Dependency graph / Dependabot). You can also configure automerge policy in the dependabot config.
