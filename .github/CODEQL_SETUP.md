# CodeQL

This repository includes a CodeQL workflow at `.github/workflows/codeql-analysis.yml` which runs on pushes, PRs, and weekly.

To enable CodeQL alerts and monitor findings, ensure the repository has "Dependabot Alerts" and code scanning enabled in Security settings.
